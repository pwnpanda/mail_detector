<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Mailbox" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/top</item>
        <!-- Primary brand color dark. -->
        <item name="colorPrimaryVariant">@color/super_red</item>
        <!-- Primary brand color light. Highlight -->
        <item name="colorOnPrimary">@color/highlight</item>
        <!-- Secondary brand color. For BG -->
        <item name="colorSecondary">@color/background</item>
        <!-- Secondary brand color dark. For button dark -->
        <item name="colorSecondaryVariant">@color/background_dark_blue</item>
        <!-- Secondary brand color light. For Button light -->
        <item name="colorOnSecondary">@color/button</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/charcoal</item>
        <!-- Customize your theme here. -->
        <item name="android:actionMenuTextColor">@color/charcoal</item>
        <item name="iconSize">2dp</item>
        <item name="itemIconSize">2dp</item>
    </style>

    <style name="TextAppearance.AppCompat.Widget.ActionBar.Title" parent="@style/TextAppearance.AppCompat">
        <item name="android:textSize">28dp</item>
        <item name="android:textColor">@color/charcoal_light</item>
        <item name="android:fontFamily">@font/alegreya_sans</item>
        <item name="android:textStyle">bold</item>
    </style>

</resources>