<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Mailbox" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/red</item>
        <!-- Primary brand color dark. -->
        <item name="colorPrimaryVariant">@color/super_red</item>
        <!-- Primary brand color light. -->
        <item name="colorOnPrimary">@color/light_red</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/background_blue</item>
        <!-- Secondary brand color dark. -->
        <item name="colorSecondaryVariant">@color/background_dark_blue</item>
        <!-- Secondary brand color light. -->
        <item name="colorOnSecondary">@color/background_light_blue</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/charcoal</item>
        <!-- Customize your theme here. -->
        <item name="android:actionMenuTextColor">@color/charcoal</item>
    </style>
    <style name="TextAppearance.AppCompat.Widget.ActionBar.Title" parent="@style/TextAppearance.AppCompat">
        <item name="android:textSize">28dp</item>
        <item name="android:textColor">@color/charcoal</item>
        <item name="android:fontFamily">@font/alegreya_sans</item>
        <item name="android:textStyle">bold</item>
    </style>
</resources>